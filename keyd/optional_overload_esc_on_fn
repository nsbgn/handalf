# Overload the Fn key to act as escape when tapped. There is no longer a need
# for the Escape key on the Fn layer, so this also maps the key at its position
# to LCtrl.

[main]
leftalt = timeout(overload(Fn, esc), 140, layer(Fn))

[Fn]
a = layer(control)
